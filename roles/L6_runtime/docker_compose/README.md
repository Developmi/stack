# L6 Runtime - Docker Compose

Compose plugin and project structure.

| Field  | Value       |
| ------ | ----------- |
| Status | `reference` |

## Notes

The Docker Compose plugin is installed as part of `L6_runtime/general/` (Docker Engine install).
This directory serves as a structural reference for compose-specific task overrides.
