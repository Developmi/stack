# /etc/restic/backup-paths.conf - Paths to back up by restic-backup-volumes.sh
# One path per line. Comments start with #. Empty lines are ignored.
# Paths are tarballed individually and streamed to Restic via stdin.

# Docker volumes (persistent container data)
/var/lib/docker/volumes

# System configuration
/etc

