---
# Metabase - Analytics / BI (L5)
name: metabase
version: "0.53.7"
supported_arch: [amd64, arm64]
target_group: muscle-3

depends_on: []

backup:
  schedule: "*-*-* 00/4:00:00"
  retention_daily: 7
  retention_weekly: 4
  retention_monthly: 12
  db_type: postgres
  db_name: metabase

monitoring:
  health_endpoint: "/api/health"
  health_port: 3000
  scrape_port: 3000

compliance_tags: [nist, ac-2, nist, sc-7]
dr_tier: critical
